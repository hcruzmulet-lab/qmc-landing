node_modules
.next
.env*.local
.DS_Store
dist
.claude/
# Skills/plugins de agentes (tooling local, no del proyecto)
.agents/
# Notas del harness (no del proyecto)
/memory/
# Originales pesados de marca (versiones optimizadas en code/public/clinic)
/doc/logos/
skills-lock.json
